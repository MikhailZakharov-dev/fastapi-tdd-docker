# Docker Registry Configuration
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and won't be committed

DOCKER_REGISTRY=ghcr.io
DOCKER_USERNAME=your-username
DOCKER_REPOSITORY=your-repository-name
DOCKER_IMAGE_NAME=summarizer
GITHUB_TOKEN=your-github-token

# Optional: GitHub Container Registry Token
# You can use this for docker login: echo $GITHUB_TOKEN | docker login ghcr.io -u USERNAME --password-stdin
# GITHUB_TOKEN=your-github-token-here
